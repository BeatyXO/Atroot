export type Proposal = { proposal_id:number; proposer:string; title:string; target:string; action_hash:string; intent:string; charter_version:string; status:number; confidence_band:number; rationale:string; created_at:number; evidence_url?:string; evidence_digest?:string; challenge_deadline?:number; release?:string; nonce?:number; execution_nonce?:number; method?:string; challenged?:boolean };
export const statusName = (status:number) => ['EMPTY','PROPOSED','REVIEWING','APPROVED','REJECTED','ABSTAINED','CHALLENGE WINDOW','QUEUED','EXECUTION PENDING','EXECUTED','FAILED','CANCELED'][status] ?? 'UNKNOWN';
