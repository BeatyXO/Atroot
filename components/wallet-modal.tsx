'use client';
import {useEffect,useState} from 'react';import {ChevronRight,KeyRound,LockKeyhole,Wallet as WalletIcon,X} from 'lucide-react';import {createAccount,generatePrivateKey} from 'genlayer-js';
export default function WalletModal({onClose}:{onClose:()=>void}){const [address,setAddress]=useState('');const [connected,setConnected]=useState(false);const [mode,setMode]=useState<'injected'|'browser'>('injected');const [copied,setCopied]=useState(false);useEffect(()=>{const k=localStorage.getItem('atroot.browserPrivateKey') as `0x${string}`|null;if(k){setAddress(createAccount(k).address);setMode('browser');setConnected(true)}},[]);const browser=()=>{let k=localStorage.getItem('atroot.browserPrivateKey') as `0x${string}`|null;if(!k){k=generatePrivateKey();localStorage.setItem('atroot.browserPrivateKey',k)}setAddress(createAccount(k).address);setMode('browser');setConnected(true)};const connect=async()=>{const e=(window as any).ethereum;if(!e)return alert('No injected wallet found. Install MetaMask or Rabby.');const a=await e.request({method:'eth_requestAccounts'});if(a?.[0]){setAddress(a[0]);setMode('injected');setConnected(true)}};const copy=async()=>{await navigator.clipboard.writeText(address);setCopied(true);setTimeout(()=>setCopied(false),1000)};const exportKey=()=>{const k=localStorage.getItem('atroot.browserPrivateKey')||'';const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([k],{type:'text/plain'}));a.download='atroot-private-key.txt';a.click()};return <div className="modal-backdrop" onClick={onClose}><div className="modal" onClick={e=>e.stopPropagation()}><button className="close" onClick={onClose} aria-label="Close wallet dialog"><X size={18}/></button><div className="section-kicker">IDENTITY LAYER</div><h2>{connected?'Wallet connected':'Choose your signing path.'}</h2><p className="modal-copy">Injected wallets are the default. Browser wallets are local-only and experimental.</p>{!connected&&<><button className="wallet-option" onClick={connect}><WalletIcon size={18}/><div><strong>Injected wallet</strong><span>MetaMask, Rabby, or any EIP-1193 wallet</span></div><ChevronRight size={17}/></button><button className="wallet-option" onClick={browser}><KeyRound size={18}/><div><strong>Browser wallet</strong><span>Generated locally and stored in this browser</span></div><ChevronRight size={17}/></button></>}{connected&&<div className="identity" aria-live="polite"><label>ACTIVE IDENTITY · {mode.toUpperCase()}</label><code>{address}</code><div className="identity-actions"><button onClick={copy}>{copied?'COPIED':'COPY ADDRESS'}</button>{mode==='browser'&&<button onClick={exportKey}>EXPORT PRIVATE KEY</button>}<button onClick={()=>{if(mode==='browser')localStorage.removeItem('atroot.browserPrivateKey');setConnected(false);setAddress('');onClose();window.dispatchEvent(new Event('atroot-wallet-disconnected'))}}>DISCONNECT</button></div></div>}<div className="warning"><LockKeyhole size={16}/><span>Never share private keys. Browser wallets are for development testing only.</span></div></div></div>}
