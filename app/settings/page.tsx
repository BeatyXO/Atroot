'use client';
import { useEffect,useState } from 'react';
import { createAccount,generatePrivateKey } from 'genlayer-js';
import Shell from '../../components/shell';
export default function Settings(){const [mode,setMode]=useState('injected');const [address,setAddress]=useState('');const [key,setKey]=useState('');const [copied,setCopied]=useState(false);useEffect(()=>{const eth=(window as any).ethereum;if(eth)eth.request({method:'eth_accounts'}).then((a:string[])=>a?.[0]&&setAddress(a[0]));},[]);const copy=async()=>{await navigator.clipboard.writeText(address);setCopied(true);setTimeout(()=>setCopied(false),1000)};const create=()=>{let k=localStorage.getItem('atroot.browserPrivateKey') as `0x${string}`|null;if(!k){k=generatePrivateKey();localStorage.setItem('atroot.browserPrivateKey',k)}setKey(k);setAddress(createAccount(k).address);setMode('browser')};const exportKey=()=>{const blob=new Blob([key||localStorage.getItem('atroot.browserPrivateKey')||''],{type:'text/plain'});const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='atroot-private-key.txt';a.click();URL.revokeObjectURL(a.href)};return <Shell><main className="page settings"><div className="section-kicker">CONTROL PLANE / IDENTITY</div><h1>Wallet & network</h1><p className="lede">ATROOT never stores an injected-wallet secret. Browser wallets are local-only and experimental.</p><div className="settings-grid"><section className="setting-card"><label>ACTIVE WALLET</label>{address?<><code>{address}</code><div className="identity-actions"><button className="secondary" onClick={copy}>{copied?'COPIED':'COPY ADDRESS'}</button>{mode==='browser'&&<button className="secondary" onClick={exportKey}>EXPORT PRIVATE KEY</button>}</div></>:<button className="primary" onClick={()=>{const eth=(window as any).ethereum;if(eth)eth.request({method:'eth_requestAccounts'}).then((a:string[])=>{setAddress(a[0]);setMode('injected')})}}>Connect injected wallet</button>}<span className="setting-note">Mode: {mode}</span></section><section className="setting-card"><label>BROWSER WALLET</label><p>Generate a local development identity for testing. Clearing site data destroys it.</p><button className="secondary" onClick={create}>Use browser wallet</button></section><section className="setting-card"><label>NETWORK</label><code>GenLayer StudioNet · chain 61999</code><code>https://studio.genlayer.com/api</code></section></div></main></Shell>}
